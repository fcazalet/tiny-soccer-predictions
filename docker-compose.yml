services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: worldcup_app
    restart: unless-stopped
    volumes:
      - ./src:/var/www/html
    depends_on:
      db:
        condition: service_healthy
    networks:
      - worldcup

  nginx:
    image: nginx:alpine
    container_name: worldcup_nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - worldcup

  db:
    image: mariadb:11
    container_name: worldcup_db
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: worldcup
      MYSQL_USER: worldcup
      MYSQL_PASSWORD: worldcup
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      retries: 10
    networks:
      - worldcup

  mailpit:
    image: axllent/mailpit:latest
    container_name: worldcup_mailpit
    restart: unless-stopped
    ports:
      - "8025:8025"   # Interface web Mailpit
    networks:
      - worldcup

networks:
  worldcup:
    driver: bridge

volumes:
  db_data:
